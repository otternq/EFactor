development:
  sessions:
    default:
      database: efactor
      hosts:
        - localhost:<%= ENV['BOXEN_MONGODB_PORT'] || '27017' %>
production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
      options:
        skip_version_check: true
        safe: true